# backend/requirements.txt — runtime Fase 0 (stack async; alembic sync).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# DB: async (app) + sync (alembic)
sqlalchemy>=2.0
geoalchemy2>=0.14
asyncpg>=0.29
psycopg[binary]>=3.1
alembic>=1.13

# Auth & infra
argon2-cffi>=23.1
python-jose[cryptography]>=3.3
redis>=5.0
minio>=7.2

# Uji (unit in-memory async)
pytest>=8.0
pytest-asyncio>=0.23
