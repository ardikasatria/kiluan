# Uji scaffold (murni in-memory) — cukup ini agar `pytest` hijau:
pydantic>=2.0
pytest>=8.0

# Dependensi produksi (dipakai saat brief Cursor B0 mewujudkan model + API;
# TIDAK diperlukan untuk menjalankan test suite scaffold ini):
# fastapi>=0.110
# uvicorn[standard]
# sqlalchemy>=2.0
# alembic
# geoalchemy2
# psycopg[binary]
# argon2-cffi
# redis
# minio
# python-jose[cryptography]
